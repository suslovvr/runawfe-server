package ru.runa.wfe.web.framework.core;

/**
 * Not all HTTP request methods, but only those we may want to accept.
 *
 * @author Dmitry Grigoriev (dimgel)
 */
public enum RequestMethod {
    DELETE,
    GET,
    POST,
    PUT
}
